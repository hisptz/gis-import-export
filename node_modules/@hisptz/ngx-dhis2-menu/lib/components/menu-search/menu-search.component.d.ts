import { OnInit } from '@angular/core';
import { MenuService } from '../../services/menu.service';
export declare class MenuSearchComponent implements OnInit {
    private menuService;
    rootUrl: string;
    searchWidth: number;
    showApps: boolean;
    apps: any[];
    originalApps: any[];
    loadingModules: boolean;
    filteredApp: string;
    constructor(menuService: MenuService);
    ngOnInit(): void;
    private _prepareMenuModules();
    updateMenuModules(): void;
    widdenSearch(e?: any): void;
    reduceSearch(e?: any): void;
    toggleSearch(e: any): void;
}
