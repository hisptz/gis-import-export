import { OnInit } from '@angular/core';
import { MenuService } from '../../services/menu.service';
export declare class MenuSideBarComponent implements OnInit {
    private menuService;
    rootUrl: string;
    showProfile: boolean;
    currentUser: any;
    loadingUser: boolean;
    profileMenus: any[];
    apps: any[];
    originalApps: any[];
    loadingModules: boolean;
    filteredApp: string;
    showSidebarApps: boolean;
    constructor(menuService: MenuService);
    ngOnInit(): void;
    private _prepareMenuModules();
    toggleSidebarMenus(e: any): void;
    updateMenuModules(): void;
}
