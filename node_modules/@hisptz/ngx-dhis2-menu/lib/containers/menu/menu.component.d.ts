import { OnInit } from '@angular/core';
import { Observable } from 'rxjs';
import { MenuService } from '../../services/menu.service';
import { SystemStateService } from '../../services/system-state.service';
export declare class MenuComponent implements OnInit {
    private menuService;
    private systemStatusService;
    rootUrl: string;
    applicationTitle: string;
    backgroundColor: string;
    menuLoading: boolean;
    menuLoadingFail: boolean;
    loggedIn$: Observable<boolean>;
    online: boolean;
    menuNotification: string;
    wasOffline: boolean;
    showSidebar: boolean;
    contextPath: string;
    constructor(menuService: MenuService, systemStatusService: SystemStateService);
    ngOnInit(): void;
    toggleSideBar(e: any): void;
    getSystemSettings(): void;
}
