import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
export declare class MenuNotificationService {
    private httpClient;
    constructor(httpClient: HttpClient);
    loadNotification(rootUrl: string): Observable<any>;
}
